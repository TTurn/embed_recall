milvus和faiss召回对比:
    响应时间:
        faiss单句召回时间在30ms
        milvus单句召回时间在150ms (不知是否可以通过参数配置以及GPU进行优化)
    距离计算公式:
        L2 IP 都OK；召回效果目测ok
        由于该实验中使用的向量生成模型为孪生bert，向量通过全连接之后进行分类；该方法生成的向量距离可以使用L2、IP衡量，因为L2、IP和cosine其实是等价的。

